import type { ReactNode } from "react";

function Empty(_props: { children?: ReactNode }) {
  return null;
}

export const Environment = Empty;
export const Html = Empty;
export const OrbitControls = Empty;
export const RoundedBox = Empty;
export const Text = Empty;
export const Billboard = Empty;
