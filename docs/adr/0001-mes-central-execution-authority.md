# MES is the central production-execution authority

The factory previously distributed route, NG, repair, retest, and alarm decisions across web pages and Station Agents. MES will now own these decisions behind one execution interface, while Station Agents remain edge adapters and dashboards/3D remain read projections; this reduces local autonomy but prevents contradictory product states and makes every transition auditable.

