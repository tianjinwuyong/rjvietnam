# Services

Backend services.

```text
services/
├── api/                  Main REST/JSON API
├── worker/               Background jobs, imports, scheduled tasks
└── realtime/             WebSocket/SSE events for dashboard and shop floor
```

